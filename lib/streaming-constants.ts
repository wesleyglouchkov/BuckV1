/**
 * Streaming-related constants
 */

// How often the host syncs viewer count to the database (in seconds)
// This also updates lastHeartbeat for detecting host disconnection
export const HOST_SYNC_INTERVAL_SECONDS = 60;
